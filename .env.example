# Copy to .env and fill in. Required for Gemini (default provider).

GEMINI_API_KEY=

# Optional: pydantic-ai model id (overrides default gemini-2.5-flash)
# AWESOME_O_MODEL=google-gla:gemini-2.5-flash

# Alternative key name also accepted by the Google client:
# GOOGLE_API_KEY=
